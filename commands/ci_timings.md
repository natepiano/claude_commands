---
description: Show CI job durations for recent runs on the current or specified branch
---

Run `~/.claude/scripts/ci_timings.sh $ARGUMENTS` and display the output.

- If no arguments are provided, it defaults to the current branch and shows the latest run.
- First argument: branch name
- Second argument: number of recent runs to show
