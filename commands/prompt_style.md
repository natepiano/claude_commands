Load the prompt/command style guide before writing or reviewing AI agent commands. Use **one single Bash command** to concatenate all style files and read the output. Do NOT use the Read tool on individual files.

Run this exact command with Bash:
```bash
cat ~/rust/nate_style/prompt/*.md
```

After loading, confirm: "Prompt style guide loaded — N rules across M files." Then proceed with the user's request.
