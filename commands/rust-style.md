Load the Rust style guide before writing Rust code. Read every file in `~/rust/hanadocs/topics/developer/rust/` and apply all rules when writing or reviewing Rust code in this session.

```bash
cat ~/rust/hanadocs/topics/developer/rust/*.md
```

After loading, confirm: "Rust style guide loaded — N rules across M files." Then proceed with the user's request.
