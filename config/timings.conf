# Shared workflow timing values, in seconds.

# Fixed delay between independent reporter runs. `reporter.sh watch --interval`
# overrides it. Invalid or missing values fall back to 240.
PLAN_DELEGATE_REPORTER_INTERVAL_SECONDS=240
