# Shared workflow timing values, in seconds.

# Delay between user-facing progress reports while a delegated phase is active.
# Invalid or missing values fall back to 240.
PLAN_DELEGATE_PROGRESS_INTERVAL_SECONDS=240
