<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.natemccoy.clean-fix</string>
    <!-- Fires every StartInterval seconds. The wrapper script checks HID
         idle time (via ioreg) and runs the clean-fix build whenever the user
         has been away from keyboard for >= 1 hour AND no previous run is
         still in flight (pgrep guard against the build script's path). -->
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/natemccoy/.claude/scripts/clean-fix/clean-fix-trigger.sh</string>
    </array>
    <key>StartInterval</key>
    <integer>600</integer>
    <key>StandardOutPath</key>
    <string>/tmp/clean-fix-stdout.log</string>
    <key>StandardErrorPath</key>
    <string>/tmp/clean-fix-stderr.log</string>
</dict>
</plist>
