# Clean-fix agent assignments.
#
# This file answers "which agent runs this clean-fix stage?" Global agent
# defaults and allowlists live in ~/.claude/config/agents.conf.
#
# Leave model/effort empty to use the selected agent's global defaults. Set a
# non-empty model/effort here only to override this clean-fix stage.

[style_eval]
enabled=false
agent=codex
model=
effort=

[style_eval_review]
enabled=false
agent=codex
model=
effort=

[style_fix]
enabled=false
agent=codex
model=
effort=
